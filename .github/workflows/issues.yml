name: Issues

on:
  issues:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.issue.title }}"
      - run: |
          find -name '*.sh' | xargs -I{} sh -c 'echo "{}\n====\n";cat {}'
          find -name '*.sh' | xargs -I{} cat {}
